一、
①
1
2
4
3
2
②
语句1对应定义1
语句2对应定义3
语句3对应定义2
语句4对应定义4
语句5对应定义3
③在同一个作用域内，变量名是唯一的，若同名，内层的变量会隐藏外层的，优先被访问
二、
①全局的a和命名空间x3里的a冲突了
②cout << ::a;
cout << x1::a;
cout << x1::x2::a;
三、
①<>在编译器安装目录下的头文件目录寻找，若未找到则报错
""先在当前目录找，没找到再去编译器安装目录下的头文件目录寻找，若未找到则报错
②file1加一行extern int a; 
file2加一行extern int b; 
③不合适，头文件只做声明，比如函数、变量声明，宏定义等，不做全局变量定义，否则多次引用容易引发重复定义的错误